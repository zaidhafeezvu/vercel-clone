{
  "name": "vercel-clone",
  "version": "1.0.0",
  "author": "",
  "main": "index.js",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "description": "A minimal full-stack Vercel clone with high-performance Go backend",
  "keywords": ["vercel", "clone", "deployment", "react", "go", "gin"],
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd go-server && ./bin/server",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd go-server && go build -o bin/server cmd/server/main.go",
    "install:all": "npm install && cd client && npm install && cd ../go-server && go mod tidy"
  },
  "workspaces": [
    "client"
  ]
}